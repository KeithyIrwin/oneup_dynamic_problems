What are the contents of the queue myQueue after the following statement execute? Assume that LinkedQueue is a class that implements the interface QueueInterface. List the content of the queue from front to back, separating the elements with a comma or a space.

QueueInterface myQueue = new LinkedQueue();

[|opslist|]

[{make_answer('queue','String',5,word_list_equality(answer),3,displayAns)}]
